import { StyleSheet } from 'react-native';

const WIDTH = '100%';

export default StyleSheet.create({
  mainContainer: {
    width: WIDTH,
  },
  columnContainer: {
    flexDirection: 'column',
    width: WIDTH,
  },
  rowContainer: {
    flexDirection: 'row',
    width: WIDTH,
  },
});
